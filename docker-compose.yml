manager:
    build: manager
    #hostname: manager
    ports:
        - "7180:7180"
    links:
        - db
    volumes:
        - parcel-repo:/opt/cloudera/parcel-repo

db:
    build: db

agent:
    build: agent
    #hostname: agent0
    links:
        - manager
